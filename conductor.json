{
  "scripts": {
    "setup": "cp $CONDUCTOR_ROOT_PATH/.env . && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python3 manage.py migrate",
    "run": "source venv/bin/activate && DEBUG=True python3 manage.py runserver",
    "archive": ""
  }
}
