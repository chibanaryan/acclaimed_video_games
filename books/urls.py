from django.urls import path

app_name = "books"

urlpatterns = [
    # Book views will be added in Phase 4.5
    # Example routes (to be implemented):
    # path("", views.BookHomePageView.as_view(), name="home"),
    # path("<slug:slug>/", views.BookDetailView.as_view(), name="detail"),
    # path("authors/", views.AuthorListView.as_view(), name="author_list"),
    # path("authors/<slug:slug>/", views.AuthorDetailView.as_view(), name="author_detail"),
]
