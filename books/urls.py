"""
Books app URL patterns.

This file will contain the main view URLs for the books app.
Currently, only the API URLs are set up.
"""

from django.urls import include, path

app_name = "books"

urlpatterns = [
    # API endpoints are included separately in main urls.py
    # View URLs will be added in Phase 4.5
]
